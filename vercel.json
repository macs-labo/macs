{
  "ignoreCommand": "if echo \"$VC_REMOTE_COMMIT_MESSAGE\" | grep -q \"\\[skip ci\\]\"; then exit 0; else git diff --quiet HEAD^ HEAD ./:^README.md :^.github/ :^LICENSE :^cron/; fi",
  "functions": {
    "api/index.php": {
      "runtime": "vercel-php@0.8.0",
      "includeFiles": "{macs/**/*.php,data/*.db,macs/templates/*.html}"
    }
  },
  "routes": [
    { "src": "/macs/(.*\\.(js|css|png|jpg|gif|ico|svg))$", "dest": "/macs/$1" },
    { "src": "/macs/(.*)", "dest": "/api/index.php" },
    { "handle": "filesystem" }
  ]
}
